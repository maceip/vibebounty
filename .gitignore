# virtualenvs
.venv/
.venv-train/
.venv-mlc-win/
.venv-mlc-src/
.venv-mlc/
venv/
__pycache__/
*.pyc

# compiled / native binaries (never commit)
*.dll
*.lib
*.pyd
*.so
*.dylib
*.exe
*.o
*.a
*.bin
*.gguf
*.pt
*.pth
*.onnx
*.tgz
*.tar
*.tar.gz
*.zip

# pulled model artifacts / weight bundles from the Mac or remote boxes
mac_pull/
models/
dist/

# stale one-off snapshot of remote scripts (canonical copies live in remote/ + data/)
_remote_snapshot/

# secrets / keys
.hftoken
*.pem
.env

# large data artifacts (regenerated from the corpus / pulled from the Mac)
data/corpus/*.parquet
data/corpus/*.csv
data/sft/*.jsonl
incoming_reports/
feeds/cache/*.json

# model artifacts
adapters/
vibethinker-bbtriage/
*.safetensors

# generated eval artifacts (regenerated by run_eval / validate_tune / select_checkpoint)
# NB: eval/report_viewer.html is a tool and stays tracked.
eval/report.json
eval/report.md
eval/report_model.json
eval/report_model.md
eval/report_baseline.json
eval/report_ckpt_*.json
eval/ckpt_sweep.tsv
eval/dev_slice.jsonl
eval/report_model_lambda_coldstart.json
eval/report_model_lambda_coldstart.md
eval/scoreboard.jsonl
logs/
