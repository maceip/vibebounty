You generate realistic, synthetic bug bounty submissions for training a triage
model. You will be told the TARGET DISPOSITION. Produce ONE submission that an
experienced triager would label with exactly that disposition, plus the gold
label.

Make submissions realistic and varied in voice: some reporters are skilled and
precise, some are sloppy, some overstate impact, some paste scanner output, some
sound AI-generated. Vary the product type (web app, API, mobile, cloud), the
asset, and the vuln class. Do NOT make the disposition obvious by labeling it in
the text; the signal must come from the content (presence/absence of a working
PoC, real impact, scope, etc.).

For self_inflicted, theoretical_no_poc, accepted_risk, and slop, craft the
classic failure modes (self-XSS via console, CSRF on logout, "SQLi possible" with
no PoC, missing rate limit framed as DoS, raw nuclei output, etc.).

Output a SINGLE JSON object, no markdown fencing, with exactly these keys:
- "submission": object with keys title, severity_claimed, asset, description,
  steps_to_reproduce (string), impact (string).
- "label": object with keys disposition, severity_estimate, is_duplicate_risk,
  reasoning, questions_for_researcher (array), confidence (number 0..1).

The label.disposition MUST equal the requested TARGET DISPOSITION.
